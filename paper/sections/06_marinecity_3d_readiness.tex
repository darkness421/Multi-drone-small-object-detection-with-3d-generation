\subsection{MarineCity 3D Benchmark Protocol}
\label{sec:marinecity_3d_protocol}

This section reports the current MarineCity 3D/reasoner protocol and the
verified real-Cesium validation artifacts. We do not present this protocol as a
full benchmark result: it validates scene loading, multi-UAV capture,
SAFR-YOLO EvidenceToken export, 3D evidence grouping, and AeroGraph Reasoner
interface wiring. This boundary is important: the 2D detector is evaluated as a
completed quantitative model claim, while the graph, 3D completion, and
reasoner stages are reported as reproducible system-protocol evidence.

The scene is centered on the Haeundae MarineCity region and is constructed in
Isaac Sim with Cesium geospatial context. The current verified protocol uses
three UAV views over the saved real-Cesium \texttt{uavmarine.usd} stage. The UAV
camera altitude band is fixed to 140--160 m for the current protocol
(the latest viewer160 recapture uses 140/150/160 m for UAV~1/2/3 across
S0--S2), which is logged separately from the Cesium georeference height. Each UAV exports
RGB/depth-like capture artifacts, camera metadata, pose hints, and timestamp
fields. Vehicles and pedestrian-like small objects are placed from the detector
class vocabulary so that the 2D SAFR-YOLO output can be converted into
structured EvidenceTokens for the 3D graph and \textbf{AeroGraph Reasoner}.
The current actor-layer audit confirms six VisDrone-derived object cards
(vehicle classes plus pedestrian/person actors), three UAV markers, and three
UAV camera prims on the real-Cesium stage without synthetic city replacement.
The current SAFR-YOLO validation runs produce detector EvidenceTokens for cars,
buses, vans, and pedestrians from the UAV views. The latest integration audit
reports 78 detector tokens over the three viewer160 scenarios: 53 cars, 17
buses, 6 pedestrians, and 2 vans. The updated ambiguity-policy diagnostic groups
these tokens into 17 graph-level object hypotheses and assigns verified actions
with the ambiguity-aware decision policy described in the method section. The compact
system tables below are intended as pipeline validation rather than as a final
class-balanced quantitative benchmark.

The supplementary material summarizes the real-Cesium capture source now used
for the 3D/reasoner handoff. We intentionally report this as capture evidence
rather than as a full neural reconstruction benchmark. We additionally run
short Nerfstudio runner-family validation experiments
on the exported MarineCity views to verify that neural 3D methods can be
trained and evaluated on the scene. The current verified rows cover Nerfacto,
Instant-NGP, and a Splatfacto/3DGS-style runner under the same sparse
real-Cesium capture split. These rows are system-executability evidence rather
than a final optimized 3D benchmark; longer validation is kept as an optional
late-stage improvement.
To verify that the captures are usable before installing the upstream neural
3D runners, we also export a NeRF-style \texttt{transforms.json} package and a
depth-fused colored point-cloud validation artifact from the same nine
real-Cesium views. This point cloud is used only as a geometry handoff sanity
check in the supplementary material; it is not reported as a neural
completion/reconstruction result.

We also export the real-Cesium detector outputs into a cross-view evidence graph
for the current validation protocol. The graph builder consumes the 78 SAFR-YOLO
EvidenceTokens from the three-scenario, three-UAV viewer160 capture, performs
camera-metadata-based approximate ray projection, and groups them into 17
object hypotheses. Eight hypotheses receive multi-view support, while the graph
records 78 support edges, 11 conflict edges, and 25 missing-evidence edges for
downstream ambiguity handling. The action distribution is 0 finalize, 1
monitor, 7 reject, and 9 targeted re-observation hypotheses. This is a protocol
validation of evidence association and uncertainty-aware action selection, not a completed
metric 3D reconstruction. The detailed graph table is placed in the
supplementary material.

The protocol reserves train, validation, and held-out unseen-angle test splits
with multiple UAV viewpoints, including nadir, front-oblique, side, rear-oblique,
right-oblique, and left-oblique cameras. The held-out split emphasizes
unseen viewpoints so that the reconstruction and reasoning components are tested
on viewpoint generalization rather than memorized camera poses. The first
validation run focuses on a small number of manually verified scenes rather than a
large synthetic dataset claim.

\paragraph{3D and reasoner evaluation protocol.}
The validated protocol already runs: (1) SAFR-YOLO on each UAV view, (2)
detector outputs as EvidenceTokens, (3) token grouping into 3D evidence
hypotheses, and (4) \textbf{AeroGraph Reasoner}, the drone-specialized
graph-grounded LLM/VLM reasoner, to propose verified class/action decisions
under a safety-verifier schema. In this paper, the reasoner result is used to
validate the action schema and verifier logic. We therefore report the graph
action policy, allowed actions, and verifier behavior rather than claiming a
standalone language-model benchmark. For 3D
completion, Table~\ref{tab:marinecity_3d_completion_results} now contains three
completed validation rows on the real-Cesium capture package: Nerfacto,
Instant-NGP, and a Splatfacto/3DGS-style runner. These rows demonstrate that the
neural-3D handoff is executable across multiple runner families; they are not
claimed as a full optimized 3D benchmark until longer validation and cleaner
reconstruction captures are completed.
Table~\ref{tab:marinecity_system_validation} summarizes the completed validation
artifacts. The current AeroGraph prompt/schema validation details
remain in the supplementary material as validation support rather than as the
headline system result.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\caption{MarineCity neural 3D completion validation metrics on held-out real-Cesium
MarineCity views. Only completed runner rows are included; these rows verify
runner-family execution rather than claim a final optimized benchmark.}
\label{tab:marinecity_3d_completion_results}
\input{tables/marinecity_3d_completion_results_table}
\end{table}

\paragraph{3D completion result analysis.}
Table~\ref{tab:marinecity_3d_completion_results} provides the current
neural-3D evidence that the real-Cesium capture package can be consumed by
standard reconstruction runners. Nerfacto gives the strongest held-out image
quality in this sparse-view validation setting, reaching 22.53 PSNR, 0.944 SSIM, and
0.072 LPIPS after the 24k full-resolution validation run. The 3DGS-style row is
substantially faster at rendering but lower in reconstruction quality, while the
Instant-NGP row sits between these extremes. We therefore use this table to
support system feasibility and runner-family coverage, not to claim that the
MarineCity scene is a fully optimized 3D benchmark.

\input{tables/marinecity_system_scenario_table}

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\caption{MarineCity ambiguity-aware decision efficiency for the completed real-Cesium
viewer160 validation protocol. $\bar{A}$ is mean ambiguity; S/C/M are
support/conflict/missing-evidence edge counts; F/M/R/O are
finalize/monitor/reject/targeted-re-observation action counts. Saved \% is the
reduction from raw detector tokens to graph-level decision candidates.}
\label{tab:marinecity_system_efficiency}
\input{tables/marinecity_system_efficiency_table}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[trim=0 345 0 0,clip,width=0.98\linewidth]{figures/results/marinecity_system/s2_scenario_panel.png}
\caption{Representative qualitative MarineCity system-validation evidence for
the coastline multi-view ambiguity case. The cropped panel shows real-Cesium UAV
views and SAFR-YOLO detector overlays used to form cross-view EvidenceTokens,
while the quantitative graph and action-policy counts are reported in
Table~\ref{tab:marinecity_system_efficiency}.}
\label{fig:marinecity_main_qualitative}
\end{figure}

\paragraph{System-validation result analysis.}
The qualitative evidence in Fig.~\ref{fig:marinecity_main_qualitative} connects
the numerical rows to the actual system pipeline: the saved real-Cesium
MarineCity stage is rendered from three UAV viewpoints, SAFR-YOLO produces
object-level EvidenceTokens, and the same tokens are converted into
cross-view graph inputs. Table~\ref{tab:marinecity_system_efficiency} further
shows why the graph layer is useful before action selection: it compresses 78
token-level detections into 17 ambiguity-aware hypotheses, reducing review
candidates by 61 under the current validation protocol. The high mean ambiguity, 11 conflict
edges, and 25 missing-evidence edges explain why most non-rejected hypotheses
are sent to targeted re-observation rather than directly finalized. The single
monitor action appears in the coastline multi-view case where a bus hypothesis
has three-view support and moderate ambiguity. The figure is therefore used as
a visual sanity check for geometry-aware evidence flow. It complements the
detector table, where the 2D model is evaluated quantitatively, and the 3D
table, where the neural runner handoff is evaluated through image-quality
metrics.

\paragraph{Claim boundary.}
The main paper reports the verified real-Cesium capture, detector-token,
evidence-graph, action-policy, and neural-3D system-validation artifacts. We do not claim a
large-scale 3D reconstruction benchmark or an unconstrained autonomous UAV
planner; the reasoner is evaluated through its graph-grounded action interface
and verifier-controlled outputs.
