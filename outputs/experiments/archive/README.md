# Experiment Archive

These files are kept for traceability, not as the current paper result.

- `server_smoke/`: early environment and smoke-test summaries.
- `server_official/`: older official snapshot from the first server run.
- `initial_server_baselines/`: superseded baseline CSVs and generated job scripts before the fresh/top-3 queue.

For current tables, use the files one level above this directory.
